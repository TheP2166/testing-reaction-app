<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Chain Reaction - Studio</title>
    <!-- MediaPipe Segmentation Engine -->
    <script src="https://jsdelivr.net" crossorigin="anonymous"></script>
    <style>
        :root {
            --primary: #ff2a54; /* TikTok red accent */
            --bg-dark: #09090b;
        }
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            -webkit-tap-highlight-color: transparent;
        }
        body {
            background-color: var(--bg-dark);
            color: #fff;
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            overflow: hidden;
        }
        /* Mobile Viewport Container */
        .studio-container {
            position: relative;
            width: 100%;
            max-width: 480px;
            height: 100vh;
            background: #000;
            display: flex;
            flex-direction: column;
            overflow: hidden;
        }
        #webcam {
            display: none;
        }
        #outputCanvas {
            width: 100%;
            height: 100%;
            object-fit: cover;
            position: absolute;
            top: 0;
            left: 0;
            z-index: 1;
        }
        /* UI Interface Layer */
        .interface-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: 2;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
            padding: 20px;
            pointer-events: none;
            background: linear-gradient(to bottom, rgba(0,0,0,0.4) 0%, transparent 15%, transparent 80%, rgba(0,0,0,0.6) 100%);
        }
        .interface-overlay * {
            pointer-events: auto;
        }
        /* Top Navigation Bar */
        .top-bar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            width: 100%;
        }
        .close-btn {
            background: rgba(255,255,255,0.2);
            border: none;
            color: #fff;
            font-size: 1.2rem;
            width: 40px;
            height: 40px;
            border-radius: 50%;
            cursor: pointer;
            backdrop-filter: blur(10px);
        }
        .timer-badge {
            background: rgba(0, 0, 0, 0.6);
            padding: 6px 14px;
            border-radius: 20px;
            font-size: 0.9rem;
            font-weight: 600;
            letter-spacing: 1px;
            border: 1px solid rgba(255,255,255,0.1);
        }
        /* Countdown Overlay */
        .countdown-display {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            font-size: 6rem;
            font-weight: 800;
            color: #fff;
            z-index: 5;
            display: none;
            text-shadow: 0 4px 20px rgba(0,0,0,0.5);
        }
        /* Bottom Control Panel */
        .bottom-panel {
            display: flex;
            flex-direction: column;
            gap: 20px;
            width: 100%;
            align-items: center;
        }
        /* AI Scenery Carousel Selector */
        .scenery-carousel {
            display: flex;
            gap: 12px;
            overflow-x: auto;
            width: 100%;
            padding: 5px 10px;
            scroll-behavior: smooth;
        }
        .scenery-carousel::-webkit-scrollbar {
            display: none;
        }
        .scenery-card {
            flex: 0 0 75px;
            height: 75px;
            border-radius: 14px;
            border: 2px solid transparent;
            overflow: hidden;
            cursor: pointer;
            transition: all 0.2s ease;
            position: relative;
        }
        .scenery-card img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        .scenery-card.active {
            border-color: var(--primary);
            transform: scale(1.05);
        }
        .scenery-label {
            position: absolute;
            bottom: 4px;
            left: 0;
            width: 100%;
            font-size: 0.6rem;
            text-align: center;
            background: rgba(0,0,0,0.5);
            padding: 2px 0;
        }
        /* Capture Shutter System */
        .shutter-container {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 100%;
            margin-bottom: 10px;
        }
        .shutter-outer {
            width: 84px;
            height: 84px;
            border-radius: 50%;
            border: 4px solid #fff;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: all 0.2s ease;
        }
        .shutter-inner {
            width: 66px;
            height: 66px;
            border-radius: 50%;
            background-color: var(--primary);
            transition: all 0.3s ease;
        }
        .recording .shutter-outer {
            border-color: rgba(255,255,255,0.4);
        }
        .recording .shutter-inner {
            transform: scale(0.6);
            border-radius: 8px;
        }
    </style>
</head>
<body>

    <div class="studio-container">
        <!-- Live Processing Canvas Frame -->
        <canvas id="outputCanvas" width="720" height="1280"></canvas>
        <video id="webcam" autoplay playsinline></video>
        
        <div id="countdown" class="countdown-display">3</div>

        <!-- UI Interactions Layout -->
        <div class="interface-overlay">
            <div class="top-bar">
                <button class="close-btn" onclick="location.reload()">✕</button>
                <div id="timer" class="timer-badge">00:00</div>
                <div style="width: 40px;"></div> <!-- Layout balance element -->
            </div>

            <div class="bottom-panel">
                <!-- AI Horizontal Environment Engine Scroller -->
                <div class="scenery-carousel">
                    <div class="scenery-card active" onclick="selectScenery(this, 'https://unsplash.com')">
                        <img src="https://unsplash.com">
                        <div class="scenery-label">Cyberpunk</div>
                    </div>
                    <div class="scenery-card" onclick="selectScenery(this, 'https://unsplash.com')">
                        <img src="https://unsplash.com">
                        <div class="scenery-label">Fantasy</div>
                    </div>
                    <div class="scenery-card" onclick="selectScenery(this, 'https://unsplash.com')">
                        <img src="https://unsplash.com">
                        <div class="scenery-label">Sci-Fi Space</div>
                    </div>
                    <div class="scenery-card" onclick="selectScenery(this, 'https://unsplash.com')">
                        <img src="https://unsplash.com">
                        <div class="scenery-label">Anime</div>
                    </div>
                </div>

                <!-- Record Processing Center -->
                <div class="shutter-container">
                    <div class="shutter-outer" id="shutterBtn" onclick="toggleRecording()">
                        <div class="shutter-inner"></div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        const video = document.getElementById('webcam');
        const canvas = document.getElementById('outputCanvas');
        const ctx = canvas.getContext('2d');
        const shutterBtn = document.getElementById('shutterBtn');
        const timerElement = document.getElementById('timer');
        const countdownElement = document.getElementById('countdown');

        let backgroundImg = new Image();
        backgroundImg.crossOrigin = "anonymous";
        backgroundImg.src = 'https://unsplash.com';

        let mediaRecorder;
        let recordedChunks = [];
        let isRecording = false;
        let startTime;
        let timerInterval;

        // Setup AI Background Segmentation
        const selfieSegmentation = new SelfieSegmentation({
            locateFile: (file) => `https://jsdelivr.net{file}`
        });
        selfieSegmentation.setOptions({ modelSelection: 1 });
        selfieSegmentation.onResults(renderCompositeVideo);

        async function initCamera() {
            try {
                const stream = await navigator.mediaDevices.getUserMedia({
                    video: { width: 1280, height: 720, facingMode: "user" },
                    audio: true
                });
                video.srcObject = stream;
                video.addEventListener('loadedmetadata', () => requestAnimationFrame(processStream));
            } catch (err) {
                alert("Camera access required for AI background mapping.");
            }
        }

        async function processStream() {
